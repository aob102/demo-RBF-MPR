tasks:
  Run all Suites:
    shell: python -m robot --report NONE --outputdir output TESTS/Exec1

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
